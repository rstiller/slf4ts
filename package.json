{
    "name": "slf4ts",
    "private": true,
    "devDependencies": {
        "@types/chai": "^4.2.10",
        "@types/chai-string": "^1.4.2",
        "@types/elasticsearch": "^5.0.36",
        "@types/mocha": "^7.0.2",
        "@types/node": "^13.7.7",
        "@typescript-eslint/parser": "2.23.0",
        "@typescript-eslint/eslint-plugin": "2.23.0",
        "chai": "^4.2.0",
        "chai-string": "^1.5.0",
        "eslint": "6.8.0",
        "eslint-config-standard-with-typescript": "14.0.0",
        "eslint-plugin-standard": "4.0.1",
        "eslint-plugin-promise": "4.2.1",
        "eslint-plugin-import": "2.20.1",
        "eslint-plugin-node": "11.0.0",
        "husky": "4.2.3",
        "lerna": "^3.20.2",
        "lodash": "^4.17.15",
        "mocha": "^7.1.0",
        "mocha-typescript": "^1.1.17",
        "nyc": "^15.0.0",
        "pkgsign": "^0.2.0",
        "rimraf": "^3.0.2",
        "testdouble": "^3.13.0",
        "ts-node": "^8.6.2",
        "typescript": "^3.8.3"
    },
    "scripts": {
        "bootstrap": "lerna bootstrap",
        "bootstrap-run": "lerna run --loglevel=silent --no-prefix --scope",
        "build": "lerna run build",
        "sign": "lerna run sign && git add packages/*/signature.json",
        "clean": "lerna run clean",
        "compile": "lerna run compile",
        "coverage": "lerna run coverage",
        "deps": "lerna run deps",
        "lint": "lerna run lint",
        "publish": "lerna publish",
        "publish-dry-run": "lerna publish --skip-npm --no-git-tag-version --no-push",
        "pretest": "lerna run compile --scope slf4ts-api",
        "publishDocs": "cd docs && git add --all . && git commit -nm 'Deploy to gh-pages' && git push origin gh-pages && cd ..",
        "prepublishDocs": "docker-compose run --rm docs",
        "test": "lerna run test"
    },
    "husky": {
        "hooks": {
            "pre-commit": "npm run sign"
        }
    }
}
