{
    "name": "slf4ts",
    "private": true,
    "devDependencies": {
        "lerna": "^3.6.0"
    },
    "scripts": {
        "bootstrap": "lerna exec npm install && lerna link",
        "pretest": "lerna run compile --scope slf4ts-api",
        "test": "lerna run test"
    }
}
